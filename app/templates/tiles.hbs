{{this.model.activity.title}} : {{this.model.activity.description}}

<br/>
<br/>

{{#if this.model.image}}
  <img src="{{this.model.image}}" alt="" />
{{/if}}

<ImageUploader
  @tile={{this.model}}
  @didUpload={{this.successfulUpload}}
/>

{{outlet}}
