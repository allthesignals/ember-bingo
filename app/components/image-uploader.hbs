<FileUpload ...attributes
  @name="photos"
  @accept="image/*"
  @onfileadd={{action this.handleFile}} as |queue|
>
  {{#if queue.files.length}}
    Uploading...
  {{else}}
    {{yield}}
  {{/if}}

</FileUpload>
